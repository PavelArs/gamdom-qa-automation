{
  "name": "gamdom-qa-automation",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "test": "bunx playwright test --ui",
    "test:ui": "bun run test -- --project=ui",
    "test:api": "bun run test -- --project=api",
    "report": "bunx playwright show-report"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
